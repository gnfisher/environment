#!/usr/bin/env bash
set -euo pipefail

# ws - Copilot worktree + tmux session manager
#
# Conventions:
# - Worktrees live under ~/.copilot/copilot-worktrees/<repo>/...
# - PR worktrees: <repo>/pr-<number>
# - Named worktrees: <repo>/<user>/<session-slug>

WORKTREE_BASE="${WS_WORKTREE_BASE:-$HOME/.copilot/copilot-worktrees}"
COPILOT_DB="${WS_COPILOT_DB:-$HOME/.copilot/data.db}"
WS_USER="${WS_USER:-$USER}"
WS_OPEN_COPILOT="${WS_OPEN_COPILOT:-1}"
WS_OPEN_LAZYGIT="${WS_OPEN_LAZYGIT:-1}"

REPO_ORG=""
REPO_NAME=""
REPO_DIR=""
REPO_WT_DIR=""
LOCK_PATH=""

show_help() {
    cat << 'EOF'
ws - Copilot worktree + tmux session manager

Usage: ws <command> [options]

Commands:
  new <session-slug>        Create named worktree from default branch
  new --pr <n|url>          Create/reuse PR worktree (pr-<number>)
  pr <n|url>                Shortcut for: ws new --pr <n|url>
  open <name|path>          Open an existing worktree in tmux
  list                      List managed worktrees for current repo (default: simple view)
  list --all                List managed worktrees across repos
  list --full               Include tmux session + Copilot session metadata
  list --json               Emit JSON for scripts/fzf/tmux pipelines
  delete <name|path>        Remove one managed worktree + tmux session
  clean                     Remove all managed worktrees for current repo

Notes:
  - Source repo is resolved from ~/Development/<owner>/<repo> when needed.
  - On open, ws starts Copilot with --resume=<session-id> when it finds
    a prior session mapped to that checkout path in ~/.copilot/data.db.

Options:
  -h, --help    Show this help message

Examples:
  ws new oauth-refresh-investigation
  ws pr 426638
  ws new --pr https://github.com/github/github/pull/426638
  ws open pr-426638
  ws list
  ws list --full
  ws list --all --json
  ws delete pr-426638
EOF
}

log() { echo "==> $*"; }
error() { echo "Error: $*" >&2; }

cleanup_lock() {
    if [[ -n "${LOCK_PATH:-}" ]]; then
        rmdir "$LOCK_PATH" 2>/dev/null || true
    fi
}

canonical_dir() {
    local dir="$1"
    (cd "$dir" 2>/dev/null && pwd -P)
}

path_under_base() {
    local path="$1"
    [[ "$path" == "${WORKTREE_BASE}/"* ]]
}

ensure_managed_path() {
    local path="$1"
    if ! path_under_base "$path"; then
        error "Refusing unmanaged path: $path"
        echo "Expected path under ${WORKTREE_BASE}/" >&2
        exit 1
    fi
}

validate_slug() {
    local slug="$1"
    if [[ "$slug" == */* ]]; then
        error "Invalid session slug '$slug': slashes are not allowed."
        exit 1
    fi
    if [[ ! "$slug" =~ ^[A-Za-z0-9][A-Za-z0-9._-]*$ ]]; then
        error "Invalid session slug '$slug': use letters, numbers, '.', '_' or '-'."
        exit 1
    fi
}

sql_escape() {
    printf '%s' "$1" | sed "s/'/''/g"
}

short_hash() {
    local text="$1"
    printf '%s' "$text" | shasum | awk '{print substr($1, 1, 6)}'
}

acquire_lock() {
    local key="$1"
    local lock_root="${XDG_CACHE_HOME:-$HOME/.cache}/ws/locks"
    mkdir -p "$lock_root"
    LOCK_PATH="${lock_root}/${key}.lock"
    if ! mkdir "$LOCK_PATH" 2>/dev/null; then
        error "Another ws operation is already running for this target."
        exit 1
    fi
    trap cleanup_lock EXIT
}

detect_repo_info() {
    local url
    url=$(git remote get-url origin 2>/dev/null) || {
        error "Not in a git repository or no 'origin' remote"
        exit 1
    }
    url="${url%.git}"

    if [[ "$url" =~ ^git@[^:]+:([^/]+)/([^/]+)$ ]]; then
        REPO_ORG="${BASH_REMATCH[1]}"
        REPO_NAME="${BASH_REMATCH[2]}"
    elif [[ "$url" =~ ^https://[^/]+/([^/]+)/([^/]+)$ ]]; then
        REPO_ORG="${BASH_REMATCH[1]}"
        REPO_NAME="${BASH_REMATCH[2]}"
    elif [[ "$url" =~ ^ssh://git@[^/]+/([^/]+)/([^/]+)$ ]]; then
        REPO_ORG="${BASH_REMATCH[1]}"
        REPO_NAME="${BASH_REMATCH[2]}"
    else
        error "Cannot parse origin URL: $url"
        exit 1
    fi

    REPO_DIR=$(git rev-parse --show-toplevel)
    REPO_DIR=$(canonical_dir "$REPO_DIR")
    REPO_WT_DIR="${WORKTREE_BASE}/${REPO_NAME}"
}

set_repo_from_owner_repo() {
    local owner="$1"
    local repo="$2"
    local repo_dir="$HOME/Development/${owner}/${repo}"

    if [[ ! -d "${repo_dir}/.git" ]]; then
        error "Local clone not found at ${repo_dir}"
        echo "Clone it first: gh repo clone ${owner}/${repo} ${repo_dir}" >&2
        exit 1
    fi

    REPO_ORG="$owner"
    REPO_NAME="$repo"
    REPO_DIR=$(canonical_dir "$repo_dir")
    REPO_WT_DIR="${WORKTREE_BASE}/${REPO_NAME}"
}

get_default_branch() {
    git -C "$REPO_DIR" symbolic-ref refs/remotes/origin/HEAD 2>/dev/null \
        | sed 's@^refs/remotes/origin/@@' || echo "main"
}

branch_exists() {
    local repo_dir="$1"
    local branch="$2"
    git -C "$repo_dir" show-ref --verify --quiet "refs/heads/${branch}"
}

worktree_registered_in_repo() {
    local repo_root="$1"
    local wt_path="$2"
    git -C "$repo_root" worktree list --porcelain 2>/dev/null \
        | sed -n 's/^worktree //p' \
        | grep -Fxq "$wt_path"
}

worktree_path_for_named() {
    local name="$1"
    echo "${REPO_WT_DIR}/${WS_USER}/${name}"
}

worktree_path_for_pr() {
    local pr_number="$1"
    echo "${REPO_WT_DIR}/pr-${pr_number}"
}

repo_name_from_worktree_path() {
    local wt_path="$1"
    local rel="${wt_path#${WORKTREE_BASE}/}"
    echo "${rel%%/*}"
}

worktree_display_name() {
    local wt_path="$1"
    local repo="$2"
    local prefix="${WORKTREE_BASE}/${repo}/"
    if [[ "$wt_path" == "$prefix"* ]]; then
        echo "${wt_path#$prefix}"
    else
        basename "$wt_path"
    fi
}

tmux_session_name_from_path() {
    local repo="$1"
    local display="$2"
    local wt_path="$3"
    local base="${repo}#${display}"
    local safe
    safe=$(echo "$base" | tr './: ' '_---')
    echo "${safe}-$(short_hash "$wt_path")"
}

latest_copilot_session_for_path() {
    local wt_path="$1"
    [[ -f "$COPILOT_DB" ]] || return 1
    command -v sqlite3 >/dev/null 2>&1 || return 1

    local escaped
    escaped=$(sql_escape "$wt_path")

    local query
    query=$(cat <<SQL
SELECT COALESCE(
  (
    SELECT wss.active_session_id
    FROM workspace_checkout_bindings wcb
    JOIN workspace_session_state wss ON wss.workspace_id = wcb.workspace_id
    WHERE wcb.checkout_path = '${escaped}'
      AND wss.active_session_id IS NOT NULL
      AND wss.active_session_id != ''
    LIMIT 1
  ),
  (
    WITH ranked AS (
      SELECT
        swm.session_id,
        ROW_NUMBER() OVER (ORDER BY s.updated_at DESC) AS rn
      FROM workspace_checkout_bindings wcb
      JOIN session_workspace_map swm ON swm.workspace_id = wcb.workspace_id
      JOIN sessions s ON s.id = swm.session_id
      WHERE wcb.checkout_path = '${escaped}'
    )
    SELECT session_id FROM ranked WHERE rn = 1
  )
);
SQL
)

    sqlite3 -noheader "$COPILOT_DB" "$query" 2>/dev/null | head -n1 | tr -d '[:space:]'
}

latest_copilot_session_title_for_id() {
    local session_id="$1"
    [[ -f "$COPILOT_DB" ]] || return 1
    [[ -n "$session_id" ]] || return 1
    command -v sqlite3 >/dev/null 2>&1 || return 1

    local escaped
    escaped=$(sql_escape "$session_id")
    sqlite3 -noheader "$COPILOT_DB" \
        "SELECT COALESCE(NULLIF(title, ''), '') FROM sessions WHERE id='${escaped}' LIMIT 1;" \
        2>/dev/null | head -n1
}

normalize_cell() {
    local value="$1"
    value="${value//$'\t'/ }"
    value="${value//$'\n'/ }"
    value="${value//$'\r'/ }"
    printf '%s' "$value"
}

render_table() {
    if command -v column >/dev/null 2>&1; then
        column -t -s $'\t'
    else
        cat
    fi
}

json_escape() {
    local s="$1"
    s="${s//\\/\\\\}"
    s="${s//\"/\\\"}"
    s="${s//$'\n'/\\n}"
    s="${s//$'\r'/\\r}"
    s="${s//$'\t'/\\t}"
    printf '%s' "$s"
}

output_list_rows() {
    local output_format="$1"
    local output_view="$2"
    shift 2
    local records=("$@")
    local sep=$'\x1f'

    if [[ "$output_format" == "json" ]]; then
        local first=1
        printf "[\n"
        local record
        for record in "${records[@]}"; do
            local repo name path branch tmux_status tmux_session copilot_session resume_id
            IFS="$sep" read -r repo name path branch tmux_status tmux_session copilot_session resume_id <<<"$record"
            local tmux_bool="false"
            [[ "$tmux_status" == "yes" ]] && tmux_bool="true"
            [[ "$first" -eq 1 ]] || printf ",\n"
            first=0

            if [[ "$output_view" == "simple" ]]; then
                printf '  {"repo":"%s","name":"%s","branch":"%s","tmux":%s}' \
                    "$(json_escape "$repo")" \
                    "$(json_escape "$name")" \
                    "$(json_escape "$branch")" \
                    "$tmux_bool"
            else
                printf '  {"repo":"%s","name":"%s","path":"%s","branch":"%s","tmux":%s,"tmuxSession":"%s","copilotSession":"%s","resumeId":"%s"}' \
                    "$(json_escape "$repo")" \
                    "$(json_escape "$name")" \
                    "$(json_escape "$path")" \
                    "$(json_escape "$branch")" \
                    "$tmux_bool" \
                    "$(json_escape "$tmux_session")" \
                    "$(json_escape "$copilot_session")" \
                    "$(json_escape "$resume_id")"
            fi
        done
        printf "\n]\n"
        return 0
    fi

    {
        if [[ "$output_view" == "simple" ]]; then
            printf "REPO\tNAME\tBRANCH\tTMUX\n"
        else
            printf "REPO\tNAME\tPATH\tBRANCH\tTMUX\tTMUX_SESSION\tCOPILOT_SESSION\tRESUME_ID\n"
        fi
        local record
        for record in "${records[@]}"; do
            local repo name path branch tmux_status tmux_session copilot_session resume_id
            IFS="$sep" read -r repo name path branch tmux_status tmux_session copilot_session resume_id <<<"$record"
            if [[ "$output_view" == "simple" ]]; then
                printf "%s\t%s\t%s\t%s\n" "$repo" "$name" "$branch" "$tmux_status"
            else
                printf "%s\t%s\t%s\t%s\t%s\t%s\t%s\t%s\n" \
                    "$repo" "$name" "$path" "$branch" "$tmux_status" "$tmux_session" "$copilot_session" "$resume_id"
            fi
        done
    } | render_table
}

attach_or_switch_tmux_session() {
    local session="$1"
    if [[ -z "${TMUX:-}" ]]; then
        tmux attach-session -t "$session"
    else
        tmux switch-client -t "$session"
    fi
}

tmux_open_worktree() {
    local wt_path="$1"
    wt_path=$(canonical_dir "$wt_path")

    local repo display session
    repo=$(repo_name_from_worktree_path "$wt_path")
    display=$(worktree_display_name "$wt_path" "$repo")
    session=$(tmux_session_name_from_path "$repo" "$display" "$wt_path")

    if tmux has-session -t="$session" 2>/dev/null; then
        log "Switching to tmux session '$session'..."
        attach_or_switch_tmux_session "$session"
        return 0
    fi

    log "Creating tmux session '$session'..."
    local sid
    sid=$(tmux new-session -d -P -F '#{session_id}' -c "$wt_path" -n "$repo")
    tmux rename-session -t "$sid" "$session"
    tmux set-option -t "$sid" automatic-rename off
    tmux send-keys -t "$sid" "nvim" C-m

    if [[ "$WS_OPEN_COPILOT" == "1" ]]; then
        local copilot_cmd="copilot --yolo"
        local resume_id=""
        resume_id=$(latest_copilot_session_for_path "$wt_path" || true)
        if [[ -n "$resume_id" ]]; then
            log "Resuming Copilot session: $resume_id"
            copilot_cmd="${copilot_cmd} --resume=${resume_id}"
        fi

        tmux new-window -t "$sid" -c "$wt_path" -n "copilot"
        tmux send-keys -t "${sid}:copilot" "$copilot_cmd" C-m
    fi

    if [[ "$WS_OPEN_LAZYGIT" == "1" ]]; then
        tmux new-window -t "$sid" -c "$wt_path" -n "lazygit"
        tmux send-keys -t "${sid}:lazygit" "lazygit" C-m
    fi

    tmux select-window -t "${sid}:1"
    attach_or_switch_tmux_session "$session"
}

parse_pr_ref() {
    local ref="$1"

    if [[ "$ref" =~ github\.com/([^/]+)/([^/]+)/pull/([0-9]+) ]]; then
        PR_ORG="${BASH_REMATCH[1]}"
        PR_REPO="${BASH_REMATCH[2]}"
        PR_NUMBER="${BASH_REMATCH[3]}"
    elif [[ "$ref" =~ ^[0-9]+$ ]]; then
        detect_repo_info
        PR_ORG="$REPO_ORG"
        PR_REPO="$REPO_NAME"
        PR_NUMBER="$ref"
    else
        error "Invalid PR reference: $ref"
        echo "Use a PR number (in a repo) or URL: https://github.com/owner/repo/pull/123" >&2
        exit 1
    fi
}

resolve_worktree_for_open() {
    local input="$1"

    if [[ -d "$input" ]]; then
        canonical_dir "$input"
        return 0
    fi

    detect_repo_info

    local candidate=""
    if [[ "$input" =~ ^pr-[0-9]+$ ]]; then
        candidate="${REPO_WT_DIR}/${input}"
        [[ -d "$candidate" ]] && { canonical_dir "$candidate"; return 0; }
    fi

    candidate="$(worktree_path_for_named "$input")"
    [[ -d "$candidate" ]] && { canonical_dir "$candidate"; return 0; }

    candidate="${REPO_WT_DIR}/${input}"
    [[ -d "$candidate" ]] && { canonical_dir "$candidate"; return 0; }

    local matches=()
    while IFS= read -r d; do
        matches+=("$d")
    done < <(find "$REPO_WT_DIR" -mindepth 1 -maxdepth 4 -type d -name "$input" 2>/dev/null | sort)

    if [[ ${#matches[@]} -eq 1 ]]; then
        canonical_dir "${matches[0]}"
        return 0
    fi

    if [[ ${#matches[@]} -gt 1 ]]; then
        error "Ambiguous worktree name '$input'."
        printf '  %s\n' "${matches[@]}" >&2
        exit 1
    fi

    error "Worktree '$input' not found for repo '$REPO_NAME'."
    exit 1
}

resolve_worktree_for_delete() {
    local input="$1"

    if [[ -d "$input" ]]; then
        canonical_dir "$input"
        return 0
    fi

    local matches=()
    while IFS= read -r wt; do
        matches+=("$wt")
    done < <(
        find "$WORKTREE_BASE" -mindepth 2 -maxdepth 5 -type f -name .git -exec dirname {} \; 2>/dev/null \
            | while IFS= read -r wt_path; do
                rel=""
                rel="${wt_path#${WORKTREE_BASE}/}"
                if [[ "$rel" == "$input" || "$(basename "$wt_path")" == "$input" ]]; then
                    echo "$wt_path"
                fi
            done | sort
    )

    if [[ ${#matches[@]} -eq 1 ]]; then
        canonical_dir "${matches[0]}"
        return 0
    fi
    if [[ ${#matches[@]} -gt 1 ]]; then
        error "Ambiguous worktree '$input'. Use full path or repo-relative path."
        printf '  %s\n' "${matches[@]}" >&2
        exit 1
    fi

    error "Worktree '$input' not found under ${WORKTREE_BASE}/"
    exit 1
}

cmd_new() {
    local name=""
    local pr_ref=""

    while [[ $# -gt 0 ]]; do
        case "$1" in
            --pr)
                [[ $# -ge 2 ]] || { error "--pr requires a value"; exit 1; }
                pr_ref="$2"
                shift 2
                ;;
            -*)
                error "Unknown option: $1"
                exit 1
                ;;
            *)
                name="$1"
                shift
                ;;
        esac
    done

    if [[ -n "$pr_ref" ]]; then
        parse_pr_ref "$pr_ref"
        set_repo_from_owner_repo "$PR_ORG" "$PR_REPO"

        local wt_path
        wt_path=$(worktree_path_for_pr "$PR_NUMBER")
        ensure_managed_path "$wt_path"

        if [[ -d "$wt_path" ]]; then
            log "Worktree already exists: $wt_path"
            tmux_open_worktree "$wt_path"
            return 0
        fi

        mkdir -p "$(dirname "$wt_path")"
        acquire_lock "$(short_hash "$wt_path")"

        log "Fetching PR #${PR_NUMBER} for ${PR_ORG}/${PR_REPO}..."
        git -C "$REPO_DIR" fetch origin "pull/${PR_NUMBER}/head:refs/remotes/origin/pr/${PR_NUMBER}" --quiet

        local pr_branch="pr-${PR_NUMBER}"
        if branch_exists "$REPO_DIR" "$pr_branch"; then
            log "Local branch '${pr_branch}' already exists; reusing it."
            git -C "$REPO_DIR" worktree add "$wt_path" "$pr_branch"
        else
            log "Creating worktree: $wt_path"
            git -C "$REPO_DIR" worktree add -b "$pr_branch" "$wt_path" "refs/remotes/origin/pr/${PR_NUMBER}"
        fi

        tmux_open_worktree "$wt_path"
        return 0
    fi

    [[ -n "$name" ]] || {
        error "Usage: ws new <session-slug>  or  ws new --pr <n|url>"
        exit 1
    }
    validate_slug "$name"

    detect_repo_info

    local wt_path
    wt_path=$(worktree_path_for_named "$name")
    ensure_managed_path "$wt_path"

    if [[ -d "$wt_path" ]]; then
        log "Worktree already exists: $wt_path"
        tmux_open_worktree "$wt_path"
        return 0
    fi

    mkdir -p "$(dirname "$wt_path")"
    acquire_lock "$(short_hash "$wt_path")"

    local default_branch branch
    default_branch=$(get_default_branch)
    branch="${WS_USER}/${name}"

    log "Fetching latest from origin..."
    git -C "$REPO_DIR" fetch origin --quiet

    if branch_exists "$REPO_DIR" "$branch"; then
        log "Local branch '${branch}' already exists; reusing it."
        git -C "$REPO_DIR" worktree add "$wt_path" "$branch"
    else
        log "Creating branch '${branch}' from origin/${default_branch}..."
        git -C "$REPO_DIR" worktree add -b "$branch" "$wt_path" "origin/${default_branch}"
    fi

    tmux_open_worktree "$wt_path"
}

cmd_pr() {
    [[ $# -ge 1 ]] || {
        error "Usage: ws pr <number|url>"
        exit 1
    }
    cmd_new --pr "$1"
}

cmd_open() {
    [[ $# -ge 1 ]] || {
        error "Usage: ws open <name|path>"
        exit 1
    }

    local wt_path
    wt_path=$(resolve_worktree_for_open "$1")

    if ! path_under_base "$wt_path"; then
        error "Refusing to open unmanaged path: $wt_path"
        echo "Expected path under ${WORKTREE_BASE}/" >&2
        exit 1
    fi

    tmux_open_worktree "$wt_path"
}

cmd_list() {
    local show_all=false
    local output_format="text"
    local output_view="simple"
    while [[ $# -gt 0 ]]; do
        case "$1" in
            --all) show_all=true; shift ;;
            --simple) output_view="simple"; shift ;;
            --full) output_view="full"; shift ;;
            --json) output_format="json"; shift ;;
            --format)
                [[ $# -ge 2 ]] || { error "--format requires a value: text|json"; exit 1; }
                output_format="$2"
                shift 2
                ;;
            *) error "Unknown option: $1"; exit 1 ;;
        esac
    done
    case "$output_format" in
        text|json) ;;
        *) error "Invalid --format value '$output_format' (expected: text|json)"; exit 1 ;;
    esac

    if $show_all; then
        cmd_list_all "$output_format" "$output_view"
        return
    fi

    detect_repo_info
    [[ -d "$REPO_WT_DIR" ]] || {
        echo "No managed worktrees for ${REPO_NAME}."
        return 0
    }

    local rows=()
    local sep=$'\x1f'
    while IFS= read -r line; do
        local wt_path
        wt_path=$(echo "$line" | awk '{print $1}')
        [[ "$wt_path" == "$REPO_DIR" ]] && continue
        [[ "$wt_path" != "${REPO_WT_DIR}/"* ]] && continue

        local display branch session tmux_status resume_id resume_title
        display="${wt_path#${REPO_WT_DIR}/}"
        branch=$(git -C "$wt_path" rev-parse --abbrev-ref HEAD 2>/dev/null || echo "?")
        session=$(tmux_session_name_from_path "$REPO_NAME" "$display" "$wt_path")
        tmux_status="no"
        tmux has-session -t="$session" 2>/dev/null && tmux_status="yes"
        resume_id=$(latest_copilot_session_for_path "$wt_path" || true)
        resume_title=$(latest_copilot_session_title_for_id "$resume_id" || true)
        [[ -z "$resume_title" ]] && resume_title="-"
        [[ -n "$resume_id" ]] && resume_id="${resume_id:0:12}"
        [[ -z "$resume_id" ]] && resume_id="-"

        rows+=("$(printf '%s%s%s%s%s%s%s%s%s%s%s%s%s%s%s' \
            "$(normalize_cell "$REPO_NAME")" "$sep" \
            "$(normalize_cell "$display")" "$sep" \
            "$(normalize_cell "$wt_path")" "$sep" \
            "$(normalize_cell "$branch")" "$sep" \
            "$tmux_status" "$sep" \
            "$(normalize_cell "$session")" "$sep" \
            "$(normalize_cell "$resume_title")" "$sep" \
            "$resume_id")")
    done < <(git -C "$REPO_DIR" worktree list 2>/dev/null || true)

    if [[ ${#rows[@]} -eq 0 ]]; then
        echo "No managed worktrees for ${REPO_NAME}."
        return 0
    fi

    output_list_rows "$output_format" "$output_view" "${rows[@]}"
}

cmd_list_all() {
    local output_format="${1:-text}"
    local output_view="${2:-simple}"
    [[ -d "$WORKTREE_BASE" ]] || {
        echo "No managed worktrees."
        return 0
    }

    local rows=()
    local sep=$'\x1f'
    while IFS= read -r wt_path; do
        local repo display branch session tmux_status resume_id resume_title
        repo=$(repo_name_from_worktree_path "$wt_path")
        display=$(worktree_display_name "$wt_path" "$repo")
        branch=$(git -C "$wt_path" rev-parse --abbrev-ref HEAD 2>/dev/null || echo "?")
        session=$(tmux_session_name_from_path "$repo" "$display" "$wt_path")
        tmux_status="no"
        tmux has-session -t="$session" 2>/dev/null && tmux_status="yes"
        resume_id=$(latest_copilot_session_for_path "$wt_path" || true)
        resume_title=$(latest_copilot_session_title_for_id "$resume_id" || true)
        [[ -z "$resume_title" ]] && resume_title="-"
        [[ -n "$resume_id" ]] && resume_id="${resume_id:0:12}"
        [[ -z "$resume_id" ]] && resume_id="-"

        rows+=("$(printf '%s%s%s%s%s%s%s%s%s%s%s%s%s%s%s' \
            "$(normalize_cell "$repo")" "$sep" \
            "$(normalize_cell "$display")" "$sep" \
            "$(normalize_cell "$wt_path")" "$sep" \
            "$(normalize_cell "$branch")" "$sep" \
            "$tmux_status" "$sep" \
            "$(normalize_cell "$session")" "$sep" \
            "$(normalize_cell "$resume_title")" "$sep" \
            "$resume_id")")
    done < <(find "$WORKTREE_BASE" -mindepth 2 -maxdepth 5 -type f -name .git -exec dirname {} \; 2>/dev/null | sort)

    if [[ ${#rows[@]} -eq 0 ]]; then
        echo "No managed worktrees."
        return 0
    fi

    output_list_rows "$output_format" "$output_view" "${rows[@]}"
}

cmd_delete() {
    [[ $# -ge 1 ]] || {
        error "Usage: ws delete <name|path>"
        exit 1
    }

    local wt_path
    wt_path=$(resolve_worktree_for_delete "$1")

    if ! path_under_base "$wt_path"; then
        error "Refusing to remove path outside managed root: $wt_path"
        exit 1
    fi
    if [[ ! -f "$wt_path/.git" ]]; then
        error "Not a managed git worktree: $wt_path"
        exit 1
    fi

    local repo display session
    repo=$(repo_name_from_worktree_path "$wt_path")
    display=$(worktree_display_name "$wt_path" "$repo")
    session=$(tmux_session_name_from_path "$repo" "$display" "$wt_path")

    if tmux has-session -t="$session" 2>/dev/null; then
        log "Killing tmux session '$session'..."
        tmux kill-session -t "$session"
    fi

    acquire_lock "$(short_hash "$wt_path")"

    local common_git_dir repo_root
    common_git_dir=$(git -C "$wt_path" rev-parse --path-format=absolute --git-common-dir)
    repo_root=$(dirname "$common_git_dir")
    if ! worktree_registered_in_repo "$repo_root" "$wt_path"; then
        error "Worktree is not registered in repository metadata: $wt_path"
        exit 1
    fi

    log "Removing worktree: $wt_path"
    git -C "$repo_root" worktree remove "$wt_path" --force
    git -C "$repo_root" worktree prune

    local parent
    parent=$(dirname "$wt_path")
    while [[ "$parent" == "${WORKTREE_BASE}/${repo}"* ]]; do
        rmdir "$parent" 2>/dev/null || break
        parent=$(dirname "$parent")
    done

    echo "✓ Deleted worktree: ${repo}/$(worktree_display_name "$wt_path" "$repo")"
}

cmd_clean() {
    detect_repo_info

    local paths=()
    while IFS= read -r line; do
        local wt_path
        wt_path=$(echo "$line" | awk '{print $1}')
        [[ "$wt_path" == "$REPO_DIR" ]] && continue
        [[ "$wt_path" != "${REPO_WT_DIR}/"* ]] && continue
        paths+=("$wt_path")
    done < <(git -C "$REPO_DIR" worktree list 2>/dev/null || true)

    if [[ ${#paths[@]} -eq 0 ]]; then
        echo "No managed worktrees for ${REPO_NAME}."
        return 0
    fi

    echo "Will delete ${#paths[@]} worktree(s) for ${REPO_NAME}:"
    printf '  %s\n' "${paths[@]#${REPO_WT_DIR}/}"
    echo
    read -rp "Continue? [y/N] " confirm
    [[ "$confirm" =~ ^[Yy]$ ]] || { echo "Aborted."; return 0; }

    local wt
    for wt in "${paths[@]}"; do
        cmd_delete "$wt"
    done
}

if [[ $# -eq 0 ]]; then
    show_help
    exit 0
fi

case "${1:-}" in
    -h|--help)
        show_help
        ;;
    new)
        shift
        cmd_new "$@"
        ;;
    pr)
        shift
        cmd_pr "$@"
        ;;
    open)
        shift
        cmd_open "$@"
        ;;
    list)
        shift
        cmd_list "$@"
        ;;
    delete)
        shift
        cmd_delete "$@"
        ;;
    clean)
        shift
        cmd_clean "$@"
        ;;
    *)
        error "Unknown command '${1:-}'"
        echo "Run 'ws --help' for usage" >&2
        exit 1
        ;;
esac
